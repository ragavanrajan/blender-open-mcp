# FastMCP framework for Full MCP Server mode (Python 3.10+)
fastmcp>=2.7.0

# HTTP client for Ollama and API requests
httpx>=0.24.0

# Optional: For improved async performance
uvicorn[standard]>=0.24.0

# Optional: Enhanced JSON handling
orjson>=3.9.0

# Note: Simple Server mode (Python 3.8+) requires NO additional dependencies
# It uses only Python standard library (http.server, socket, json, etc.) 